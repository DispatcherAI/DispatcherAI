/**
 * TODO: Make this based on a Websocket / Systems connection
 */
export function ConnectionStatus() {
    return (
        <div className="h-fit">
            <p className="text-xs font-bold text-dp-nonEmergency">LIVE</p>
        </div>
    );
}
